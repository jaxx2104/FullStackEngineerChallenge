import styled from "styled-components"

import { style } from "../constants"

const Label = styled.label`
  font-weight: ${style.FONT_WEIGHT};
  margin: auto ${style.SPACE_SIZE};
`

export default Label
